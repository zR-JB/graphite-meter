<div align="center">

# Graphite Meter

**A self-hosted, open-source internet speed test that keeps up with modern networks.**

One ~10 MB static binary. Multi-gigabit honest measurements. Beautiful on every screen.

[![CI](https://github.com/zR-JB/graphite-meter/actions/workflows/ci.yml/badge.svg)](https://github.com/zR-JB/graphite-meter/actions/workflows/ci.yml)
[![Release](https://img.shields.io/github/v/release/zR-JB/graphite-meter?sort=semver)](https://github.com/zR-JB/graphite-meter/releases)
[![Image](https://img.shields.io/badge/ghcr.io-zr--jb%2Fgraphite--meter-2496ed?logo=docker&logoColor=white)](https://github.com/zR-JB/graphite-meter/pkgs/container/graphite-meter)
[![License: AGPL-3.0](https://img.shields.io/badge/license-AGPL--3.0-blue)](LICENSE)

<img src="docs/assets/hero.png" alt="Graphite Meter after a finished run: 13.98 Gbit/s down, 11.08 Gbit/s up, latency-under-load profiles and per-stage throughput charts — desktop and mobile, dark mode" width="920">

</div>

Graphite Meter is built to measure the link, not the tool: in Chrome it sustains up to **60 Gbit/s down and 18 Gbit/s up**, and the native terminal client pushes **hundreds of Gbit/s**.\*

## Features

### Measurement honesty

- **Bufferbloat, measured properly** — idle latency is profiled first, then the same ping loop
  keeps running _during_ download and upload, so you see exactly how your latency degrades under
  load, with per-stage plots of average, jitter, and range for idle vs. loaded, down and up.
- **Honest numbers** — upload throughput is what the **server** actually received (byte counts
  over an elapsed clock streamed back live), never what the browser thinks it sent. All payloads
  are incompressible; stalls and reconnects can't inflate a result.
- **Honest wire-rate estimates** — opt-in forward-direction Ethernet accounting for IP, TCP/UDP,
  TLS/QUIC, HTTP framing, and explicit tunnels. The browser-facing protocol is detected
  automatically; unobservable packet details are shown as a range instead of guessed.

### Stages and adaptivity

- **Every stage is optional** — latency, download, upload, and an optional **bidirectional**
  stage that saturates both directions at once.
- **Adaptive early stopping** — a stage ends as soon as its result is statistically stable, so a
  steady fiber line finishes in seconds while a jittery link gets the full window.
- **Chunked download** — an opt-in adaptive-chunk alternative to one long download stream, an
  [experimental](docs/ARCHITECTURE.md#current-experimental-feature) browser-client setting.
- **Highly configurable** — durations, parallel streams, ping cadence, units (bits/bytes,
  SI/IEC), manual or auto gauge scaling — from the UI, no config files.

### Deployment and footprint

- **Featherweight server** — a single static Go binary (~10 MB, client embedded, `FROM scratch`
  image, no shell, no libc) with low CPU and memory draw even while sinking gigabits.
- **Independent throughput and latency transports** — Fetch throughput can use HTTP/1.1 clear,
  HTTP/1.1 TLS, HTTP/2, or HTTP/3; latency independently uses WebSocket over dedicated HTTP/1.1
  clear or TLS. Both targets are verified and frozen for the run.
- **Free and open source** — AGPL-3.0.

### Access control and clients

- **Optional private access** — an operator password (Argon2id hash generated by
  `graphite-meter hash-password`), OIDC with a group allowlist, or both at once, covering every
  UI asset, transfer, and WebSocket. See
  [Optional authentication](docs/CONFIGURATION.md#optional-authentication).
- **Native TUI client** — `graphite-meter-client` speaks the same wire protocol and runs the same
  stages against any Graphite Meter server, with its own
  [flags reference](docs/CONFIGURATION.md#native-tui-client-flags).
- **Modern, responsive UI** — dark and light themes, equally at home on a phone and a desktop.

## Quick start

Run the published image (multi-arch: amd64 + arm64):

```sh
docker run -d --name graphite-meter -p 7246:7246 ghcr.io/zr-jb/graphite-meter:latest
```

Open **http://localhost:7246** — that's it for the default clear HTTP/1.1 deployment. Ports
7246–7249, the listener each one serves, and the certificate they share are in
[Native listeners](docs/CONFIGURATION.md#native-listeners).

Use the command that matches the environment:

| Purpose                                  | Command                                                                                                                    |
| ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Local development run                    | `just dev`                                                                                                                 |
| Local production-profile run             | `just prod`                                                                                                                |
| Local run with H1-TLS, H2, and H3        | [Local TLS command](docs/DEVELOPMENT.md#local-tls-and-http3-certificates)                                                   |
| Published clear-H1 deployment            | `docker run -d --name graphite-meter -p 7246:7246 ghcr.io/zr-jb/graphite-meter:latest`                                      |
| Published H1-TLS, H2, and H3 deployment  | `GM_PUBLIC_HOST=meter.example.com docker compose -f container/docker-compose.yml -f container/docker-compose.tls.yml up -d` |
| Published deployment with authentication | `docker compose -f container/docker-compose.yml -f container/docker-compose.auth.yml up -d`                                 |

`just prod` builds the production browser profile but still runs the server from the source tree;
it is not the published deployment workflow. The base stack is
[`container/docker-compose.yml`](container/docker-compose.yml); `GM_PUBLIC_HOST` and `GM_CERT_NAME`
in the TLS overlay are Compose interpolation, described under
[Deployment scaffolding](docs/CONFIGURATION.md#deployment-scaffolding).

### Podman + systemd (Quadlet)

A ready-made unit that pulls the image and runs it as a systemd service lives at
[`container/quadlet/graphite-meter.container`](container/quadlet/graphite-meter.container).

### Native terminal client

```sh
just goclient-build            # -> go/graphite-meter-client
./go/graphite-meter-client -url https://your-server:7247 \
  -throughput-origin https://your-server:7247 \
  -latency-origin https://your-server:7247
```

An interactive TUI with the same stages (latency, download, upload, bidirectional, loaded
latency), server presets, and live telemetry — ideal for headless boxes and for pushing rates a
browser can't. Throughput and latency targets are selected and verified independently.

## Docs

Defaults need no configuration. Everything beyond them:

| Document                                                                                   | Covers                                                                              |
| ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------- |
| [docs/CONFIGURATION.md](docs/CONFIGURATION.md)                                             | Every runtime option on one page: listeners, TLS, origins, limits, auth, TUI flags.  |
| [docs/REVERSE_PROXY.md](docs/REVERSE_PROXY.md)                                              | nginx and Caddy deployments, and the headers measurement and auth require.           |
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)                                                | How server, browser client, TUI, and the `api/` contract fit together; the roadmap.  |
| [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md)                                                  | Toolchain, `just` recipes, build flags, local TLS certs, building the image.         |
| [api/wire.md](api/wire.md)                                                                  | The normative message-bus wire protocol shared by both clients and the server.       |
| [container/quadlet/README.md](container/quadlet/README.md)                                  | Podman + systemd units for the published image or a source build.                    |
| [container/quadlet/graphite-meter-tls/README.md](container/quadlet/graphite-meter-tls/README.md) | Rootless Quadlet with Let's Encrypt certificates over the Cloudflare DNS-01 challenge. |
| [container/quadlet/tailscale-sidecar/README.md](container/quadlet/tailscale-sidecar/README.md) | Serving on a tailnet through a Tailscale sidecar, without published ports.           |

Contributing, the short version: `git clone`, then `just dev`, then open http://localhost:7246.

## License

[AGPL-3.0-or-later](LICENSE).

---

<sub>\* Peak figures measured with Chrome (browser client) and the native TUI client on Linux
against a localhost server — a hardware-unconstrained best case that shows the tool won't be your
bottleneck. Across a real network, expect results limited by your NIC, path, and browser.</sub>
