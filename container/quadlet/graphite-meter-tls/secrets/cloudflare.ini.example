# Copy to cloudflare.ini, insert the restricted token, then chmod 600.
# Required Cloudflare permission: Zone / DNS / Edit, restricted to the relevant zone.
dns_cloudflare_api_token = REPLACE_WITH_CLOUDFLARE_API_TOKEN
